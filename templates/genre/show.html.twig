{% extends 'baseadmin.html.twig' %}

{% block title %}Amour Livre - Genre
{% endblock %}

{% block body %}
	<section class="section-admin w--900 p--2 bck--admin br--15 pb--1">
		<h1 class="text-center text-color-dark fw--b fsize-3 mb--1">Genre</h1>

		<table class="table">
			<tbody>
				<tr>
					<th class="text-color-dark">Id</th>
					<td>{{ genre.id }}</td>
				</tr>
				<tr>
					<th class="text-color-dark">Name</th>
					<td>{{ genre.name }}</td>
				</tr>
				<tr>
					<th class="text-color-dark">Created_at</th>
					<td>{{ genre.createdAt ? genre.createdAt|date('Y-m-d') : '' }}</td>
				</tr>
				<tr>
					<th class="text-color-dark">Updated_at</th>
					<td>{{ genre.updatedAt ? genre.updatedAt|date('Y-m-d') : '' }}</td>
				</tr>
			</tbody>
		</table>

		<div class="d--flex jc--sa">
			<a class="btn btn-secondary w--mob-70 w--200 m--1 text-center" href="{{ path('genre_index') }}">retour liste</a>

			{#<a class="btn btn-primary w--mob-70 w--200 m--1 text-center" href="{{ path('genre_edit', {'id': genre.id}) }}">éditer</a>#}

			{{ include('genre/_delete_form.html.twig') }}
		</div>
	</section>
{% endblock %}
